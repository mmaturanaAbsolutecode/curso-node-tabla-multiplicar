# Notas:

primer programa Node

```
 Options
--help         Show help                                                         [boolean]
--version      Show version number                                               [boolean]
-b, --base     Es la bse de la tabla de multiplicar                    [number] [required]
-l, --listar   Muestra la tabla de multiplicar       [boolean] [required] [default: false]
-h, --hasta    Hasta por cual numero se debe multiplicar               [number] [required]
```